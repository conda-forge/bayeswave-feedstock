From 0273d819e359de91575b7931483495b3338a52fe Mon Sep 17 00:00:00 2001
From: megmillhouse <meg.millhouse@gmail.com>
Date: Mon, 2 Mar 2026 14:51:44 -0500
Subject: [PATCH 2/2] Update megaplot to be happy with numpy 2.0

(cherry picked from commit 94544d2d9f37453ac4652eae0ada17ef8eaf4d9c)
---
 BayesWaveUtils/scripts/megaplot.py | 34 +++++++--------
 cmake-build-debug/CMakeLists.txt   | 68 ------------------------------
 2 files changed, 17 insertions(+), 85 deletions(-)
 delete mode 100644 cmake-build-debug/CMakeLists.txt

diff --git a/BayesWaveUtils/scripts/megaplot.py b/BayesWaveUtils/scripts/megaplot.py
index 8fc19ff..4e0819a 100755
--- a/BayesWaveUtils/scripts/megaplot.py
+++ b/BayesWaveUtils/scripts/megaplot.py
@@ -215,7 +215,7 @@ signal_ifoColorList = ['darkorchid','fuchsia','indigo','orchid','slateblue','med
 # ---------------------------------------------
 def get_waveform(filename):
     names = ['samples','median_waveform','50low','50high','90low','90high']
-    data = np.recfromtxt(filename,names=names)
+    data = np.genfromtxt(filename,names=names)
     return (data['samples'],data['median_waveform'],data['50low'],data['50high'],data['90low'],data['90high'])
 def read_model_file(runFlag, runType, burnin = 0, chain_num = 0):
     # read in model.dat
@@ -336,7 +336,7 @@ def get_axes_fdom(jobName, postDir, ifoList, model, time):
     
         filename = str(jobName)+postDir+'gaussian_noise_model_{0}.dat'.format(ifoNames[int(ifo)])
         
-        data = np.recfromtxt(filename,names=names)
+        data = np.genfromtxt(filename,names=names)
         
         # figure out what fmin is-- psd padded with 1's until fmin
         imin = 0
@@ -577,7 +577,7 @@ def plot_power_spectrum(jobName, postDir, ifo, plotsDir, worc, mdc, model, axwin
 
     if type == 'psd':
         filename = str(jobName)+postDir+'gaussian_noise_model_{0}.dat'.format(ifoNames[int(ifo)])
-        data = np.recfromtxt(filename, names = ['f', 'dpower', 'rpower', 'Sn'])
+        data = np.genfromtxt(filename, names = ['f', 'dpower', 'rpower', 'Sn'])
         plt.semilogy(data['f'],data['rpower'], ncolor, alpha=0.6)
         plt.ylim(min(data['rpower']),max(data['rpower']))
 
@@ -627,7 +627,7 @@ def plot_full_spectro(jobName, postDir, ifo, plotsDir, worc, mdc, models, axwinn
     names = ['f','dpower','rpower','psd']
     filename = str(jobName)+postDir+'gaussian_noise_model_{0}.dat'.format(ifoNames[int(ifo)])
 
-    data = np.recfromtxt(filename,names=names)
+    data = np.genfromtxt(filename,names=names)
     # plot data
     plt.semilogx(data['f'],data['rpower'],color=ncolor,alpha=0.5)
 
@@ -1204,11 +1204,11 @@ def plot_likelihood_1(modelList, plotsDir, runFlags = RUN_FLAGS):
         colour = 'seagreen'
         try:
             names = ['temp','likehood','error','acl']
-            data = np.recfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
+            data = np.genfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
         except:
             try:
                 names = ['temp','likehood','error']
-                data = np.recfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
+                data = np.genfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
             except:
                 pass
         #error = np.zeros(likehood.shape)
@@ -1224,11 +1224,11 @@ def plot_likelihood_1(modelList, plotsDir, runFlags = RUN_FLAGS):
             colour = set_color(mod)
             try:
                 names = ['temp','likehood','error','acl']
-                data = np.recfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
+                data = np.genfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
             except:
                 try:
                    names = ['temp','likehood','error']
-                   data = np.recfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
+                   data = np.genfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
                 except:
                     continue
             #error = np.zeros(likehood.shape)
@@ -1246,11 +1246,11 @@ def plot_likelihood_1(modelList, plotsDir, runFlags = RUN_FLAGS):
 
             try:
                 names = ['temp','likehood','error','acl']
-                data = np.recfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
+                data = np.genfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
             except:
                 try:
                     names = ['temp','likehood','error']
-                    data = np.recfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
+                    data = np.genfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
                 except:
                     print('No noise evidence')
             #error = np.zeros(likehood.shape)
@@ -1276,11 +1276,11 @@ def plot_likelihood_2(modelList, plotsDir, runFlags = RUN_FLAGS):
         colour = set_color(mod)
         try:
             names = ['temp','likehood','error','acl']
-            data = np.recfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
+            data = np.genfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
         except:
             try:
                 names = ['temp','likehood','error']
-                data = np.recfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
+                data = np.genfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
             except:
                 pass
         plt.semilogx(data['temp'], data['likehood']*data['temp'], label=mod, linewidth=2, color=colour)
@@ -1292,11 +1292,11 @@ def plot_likelihood_2(modelList, plotsDir, runFlags = RUN_FLAGS):
             colour = set_color(mod)
             try:
                 names = ['temp','likehood','error','acl']
-                data = np.recfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
+                data = np.genfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
             except:
                 try:
                    names = ['temp','likehood','error']
-                   data = np.recfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
+                   data = np.genfromtxt(str(jobName)+"{0}_evidence.dat".format(mod), names=names)
                 except:
                     continue
             plt.semilogx(data['temp'], data['likehood']*data['temp'], label=mod, linewidth=2, color=colour)
@@ -2785,9 +2785,9 @@ for mod in RUN_FLAGS.modelList:
         momentsfile = str(jobName)+postDir+'{runType}/{model}_{worc}_moments_{ifo}.dat'.format(ifo = ifoNames[int(ifo)], worc = worc,
                 model = local_model, runType = local_runType)
 
-        moments = np.recfromtxt(momentsfile, names=True)
-        if mdc: 
-            injmoments = np.recfromtxt(str(jobName) + postDir + 'injection_{1}_moments_{0}.dat'.format(ifoNames[int(ifo)], worc), names=True)
+        moments = np.genfromtxt(momentsfile, names=True, dtype=None, encoding=None)
+        if mdc:
+            injmoments = np.genfromtxt(str(jobName) + postDir + 'injection_{1}_moments_{0}.dat'.format(ifoNames[int(ifo)], worc), names=True, dtype=None, encoding=None)
         else: 
             injmoments = moments
 
diff --git a/cmake-build-debug/CMakeLists.txt b/cmake-build-debug/CMakeLists.txt
deleted file mode 100644
index 7d131d0..0000000
--- a/cmake-build-debug/CMakeLists.txt
+++ /dev/null
@@ -1,68 +0,0 @@
-#
-# CMake packaging for BayesWave
-# Copyright 2019  James Alexander Clark <james.clark@ligo.org>
-# Based on CMake packaging for libframe & Frv by Duncan MacLeod <duncan.macleod@ligo.org>
-#
-
-# -- package info -----------
-
-cmake_minimum_required(VERSION 3.12.0 FATAL_ERROR)
-project(
-    bayeswave
-    LANGUAGES C
-    VERSION 1.0.6
-    DESCRIPTION "LIGO/VIRGO burst analysis algorithm"
-    HOMEPAGE_URL "https://git.ligo.org/lscsoft/bayeswave"
-)
-
-include(GNUInstallDirs)
-include(CheckFunctionExists)
-find_package(PkgConfig)
-find_package(Git)
-find_package(OpenMP)
-
-# -- build components -------
-
-# C library | enable/disable with -DENABLE_C={yes,no} (default yes)
-add_subdirectory(src)
-#add_subdirectory(doc)
-
-# -- packaging components ---
-
-set(SPEC_IN "${CMAKE_CURRENT_SOURCE_DIR}/bayeswave.spec.in")
-set(SPEC    "${CMAKE_CURRENT_SOURCE_DIR}/bayeswave.spec")
-configure_file(${SPEC_IN} ${SPEC} @ONLY)
-
-# -- build tarball ----------
-#
-# to build a source tarball:
-#
-# mkdir dist
-# pushd dist
-# cmake ..
-# cmake --build . --target package_source
-#
-
-set(CPACK_PACKAGE_MAJOR ${${PROJECT_NAME}_MAJOR_VERSION})
-set(CPACK_PACKAGE_MINOR ${${PROJECT_NAME}_MINOR_VERSION})
-set(CPACK_PACKAGE_PATCH ${${PROJECT_NAME}_PATCH_VERSION})
-set(CPACK_PACKAGE_VERSION "${CPACK_PACKAGE_MAJOR}.${CPACK_PACKAGE_MINOR}.${CPACK_PACKAGE_PATCH}")
-
-set(CPACK_SOURCE_GENERATOR TXZ)
-set(CPACK_SOURCE_PACKAGE_FILE_NAME ${PROJECT_NAME}-${${PROJECT_NAME}_VERSION})
-set(CPACK_SOURCE_IGNORE_FILES
-    "/.*~$/"
-    ".*~$"
-    "\\\\.svn/"
-    "\\\\.git"
-    "build/"
-    "CMakeFiles/"
-    "CMakeCache.txt"
-    "_CPack_Packages/"
-    "\\\\.cmake"
-    "Makefile"
-    "\\\\.deps/"
-    "autom4te.cache/"
-    "\\\\.tar\\\\.xz"
-)
-include(CPack)
-- 
2.39.5

